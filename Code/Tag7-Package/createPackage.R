library(devtools)

# set working directory to where you want the package

create("MyPackage")

# set project options --> build tools --> package auf package folder

# compile with build tab in the right corner
